---
export interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class={`px-4 py-2 bg-gray-800 text-neutral-200 rounded-lg font-medium hover:bg-gray-700 transition-colors duration-300 ${className}`}
>
  <slot />
</span>
